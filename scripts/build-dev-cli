#!/usr/bin/env bash
# 仅维护者本地调试：从 Dockerfile.cli 构建 CLI 镜像（tag: baklib-cli:dev）
set -euo pipefail
cd "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/.."
export BAKLIB_CLI_IMAGE=baklib-cli:dev
exec docker compose -f docker-compose.cli.yml build config
